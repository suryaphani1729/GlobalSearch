import { StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    container: {flex:1, backgroundColor:'red'},
    suggestionView: {
        width:'90%',left:'5%',backgroundColor:'white'
    },
    suggestionText:{
        padding:10
    }
});

export default styles;